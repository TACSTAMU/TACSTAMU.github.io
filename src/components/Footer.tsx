const Footer = () => {
    return (
        <footer className="bg-primary-light h-24 w-full rounded-t-3xl mt-auto">
            {/* Add any content inside the footer if needed */}
        </footer>
    );
};

export default Footer;